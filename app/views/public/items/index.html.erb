<h1>商品一覧</h1><h4>（全<%= Item.ids.count %>件）</h2>

      <% @items.each do |item| %>
      <%= attachment_image_tag item, :image, :fill, 300, 300, format: 'jpeg' %><br>
      <%= item.name %><br>
      <%= item.price %><br>
      <% end %>

